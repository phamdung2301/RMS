# 2026-06-21

## Summary of Changes
- Implemented responsive navigation bar and mobile drawer layout:
  - Created a new Thymeleaf fragment [navbar.html](file:///d:/swp/src/main/resources/templates/fragments/navbar.html) that consolidates the sidebar menu, `.mobile-top-bar` and `.sidebar-overlay` into a single file.
  - Modified [styles.css](file:///d:/swp/src/main/resources/static/css/styles.css) to support responsive sliding drawer drawer menus on screens smaller than 1024px, and styled the dimming backdrop layer.
  - Refactored 9 main templates (`dashboard.html`, `employees.html`, `hr_management.html`, `inventory.html`, `kds.html`, `pos.html`, `procurement.html`, `profile.html`, `schedule.html`) to replace duplicate inline sidebars with the single navbar fragment.
  - Unified redirection and click behavior for administrative modal shortcuts (Order Logs, Branch Management, Bank configuration) across all templates so they either call native javascript functions or link back to dashboard with `?open=...` query parameters to activate the modals.
- Optimized mobile responsiveness of the Attendance page:
  - Modified [employees.css](file:///d:/swp/src/main/resources/static/css/employees.css) to adapt grid structures (`.portal-grid`) to 1 column on screens below 768px.
  - Formatted leave request buttons to stack vertically on mobile views below 768px (increased from 600px) via `.portal-action-buttons`.
  - Added horizontal scroll support to personal calendar schedule and requests list tables.
- Fixed global mobile viewport configurations:
  - Discovered that all main templates lacked the standard `<meta name="viewport" content="width=device-width, initial-scale=1.0">` tag, which prevented mobile browsers from executing CSS media queries and caused horizontal shifting and cropped buttons.
  - Added the viewport tag to `employees.html`, `dashboard.html`, `hr_management.html`, `inventory.html`, `kds.html`, `login.html`, [pos.html](file:///d:/swp/src/main/resources/templates/pos.html), `procurement.html`, `profile.html`, and `schedule.html`.
  - Wrapped user profile link (avatar/name) in `employees.html` inside a `.user-profile` container to allow proper mobile alignment and layout centering.
- Resolved mobile hamburger drawer click and toggle lock:
  - Removed dynamic mobile top bar injection and click listener conflicts from [toast.js](file:///d:/swp/src/main/resources/static/js/toast.js).
  - Centralized robust mobile drawer toggle, overlay closing, outside click, and link click event handlers inside [navbar.html](file:///d:/swp/src/main/resources/templates/fragments/navbar.html).
- Replaced placeholder statistics in reports dashboard with real PostgreSQL database data:
  - Added statistical group by JPQL queries for branch served revenues in [OrderRepository.java](file:///d:/swp/src/main/java/web/restaurant/swp/modules/pos/repository/OrderRepository.java) and best-selling dishes in [OrderDetailRepository.java](file:///d:/swp/src/main/java/web/restaurant/swp/modules/pos/repository/OrderDetailRepository.java).
  - Modified [AnalyticsController.java](file:///d:/swp/src/main/java/web/restaurant/swp/modules/analytics/controller/AnalyticsController.java) to inject `OrderDetailRepository`, calculate scaling height percentages dynamically for all branches, format shorthand revenue values (e.g. `1.8M`, `360k`), and query top 5 best sellers of the active branch.
  - Updated [dashboard.html](file:///d:/swp/src/main/resources/templates/dashboard.html) to render the branch revenue comparison chart dynamically and highlight the user's active branch column.
- Verified successful compilation build using `mvn clean compile`.
- Committed all changes to git and pushed to main:
  - Commit 1 (Auth & Profile): Added profile template and API endpoint to change password.
  - Commit 2 (Analytics): Integrated PostgreSQL database statistics for branch revenues and best-selling dishes into the dashboard.
  - Commit 3 (Responsive & Navbar Drawer): Modularized the navbar fragment, fixed hamburger click issues and attendance layout.
  - Commit 4 (Docs & Specifications): Compiled latest spec documents and exported PDF/docx/images.
  - Commit 5 (Dockerfile): Created a Dockerfile for Render deployment using multi-stage build.
  - Commit 6 (Login responsiveness): Optimized login page layout, added premium mobile background gradient and brand header, and fixed modal padding.
  - Commit 7 (2-step Mobile Login): Designed a 2-step login flow for mobile viewports using a Continue button to smoothly scroll to the form panel.
  - Commit 8 (Sidebar scroll fix): Enabled vertical scrolling (overflow-y: auto) on the mobile sliding drawer sidebar.
- Generated system accounts and credentials report document:

  - Created [Accounts_Report.md](file:///d:/swp/technical_docs/Accounts_Report.md) listing all 50 seeded users grouped by branch, roles, and default password.
  - Compiled it into a professional Word document [Accounts_Report.docx](file:///d:/swp/technical_docs/Accounts_Report.docx).
- Fixed partner form (modal) responsiveness on mobile:
  - Replaced hardcoded inline grid styling on the inputs block and branch information fields in [dashboard.html](file:///d:/swp/src/main/resources/templates/dashboard.html) with responsive classes `.grid-2-col` and `.grid-branch-fields` from [dashboard.css](file:///d:/swp/src/main/resources/static/css/dashboard.css).
  - Ensured that on screens <= 768px the grid columns stack into 1 column (using `grid-template-columns: 1fr !important`), eliminating horizontal scroll overflow and improving form usability.
  - Committed and pushed to GitHub main branch.




