# Memory - 2026-06-17

## Task: Add Leave and Forgot Clock Request Forms to Employee Portal

### Actions taken:
- Updated `employees.html` in `d:\swp\src\main\resources\templates\employees.html`:
  - Connected "Gửi Đơn Xin Nghỉ Phép" and "Xin Bổ Sung Công" buttons to modal dialogs.
  - Implemented modern, glassmorphic modals for both leave requests and forgotten clock requests.
  - Created Javascript function `openLeaveModal`, `closeLeaveModal`, `submitLeave`, `openForgotClockModal`, `closeForgotClockModal`, and `submitForgotClock`.
  - Used AJAX `fetch` with `POST` request to send JSON payload mapping to `LeaveRequestSubmit` and `ForgotClockSubmit` controllers.
  - Added dynamic status badges depending on the status of requests (APPROVED -> green, PENDING -> orange, REJECTED -> red).
  - Added a new table display panel for "Danh Sách Đơn Bổ Sung Công" (Forgot Clock Requests) mapping to the Thymeleaf variable `${forgotRequests}`.
- Triggered Maven clean compile check to ensure everything is valid.

## Task: Implement Purchase Order (PO) Creation Form

### Actions taken:
- Updated `ProcurementController.java` in `d:\swp\src\main\java\web\restaurant\swp\modules\procurement\controller\ProcurementController.java`:
  - Injected `InventoryItemRepository`.
  - Exposed the list of all raw materials (`inventoryItems`) in the `/procurement` GET route.
  - Implemented the POST endpoint `/api/procurement/po/create` receiving a `PoCreateRequest` body.
- Updated `procurement.html` in `d:\swp\src\main\resources\templates\procurement.html`:
  - Connected the "Tạo PO Mới" button to `openCreatePoModal()`.
  - Added a new glassmorphic modal box (`createPoModal`) for creating Purchase Orders.
  - Enabled configuring the delivery deadline via a date picker, restricted to today and future dates using the `min` constraint.
  - Enabled dynamic adding and removing of material line items in a table, displaying units dynamically as ingredients are selected.
  - Coded Javascript handling validation (no duplicate materials allowed, valid inputs required) and sending JSON payloads to the `/api/procurement/po/create` backend.
- Compiled the project with Maven to confirm everything runs and binds successfully.

## Task: SaaS Multi-Tenant Upgrade

### Decisions & Architecture:
- Implemented single-database multi-tenancy.
- Scoped tenant-wide unique constraints at the `@Table` level instead of the `@Column` level to support identical names, phones, SKUs, and codes across different tenants.
- Ensured tenant isolation transitively for branch-level entities by checking the logged-in user's tenant context.

### Actions taken:
- Created the `Tenant` entity and `TenantRepository` in `web.restaurant.swp.modules.tenant`.
- Added the `Tenant` relationship to the 9 core entities (`User`, `Branch`, `Category`, `Product`, `InventoryItem`, `Customer`, `Supplier`, `Promotion`, `ShiftTemplate`).
- Created `tenants.sql` and updated `branches.sql`, `users.sql`, `categories.sql`, `products.sql`, `customers.sql`, `suppliers.sql`, `promotions.sql`, `shift_templates.sql`, and `inventory_items.sql` seeders with `tenant_id` columns and value `'tenant-1'`.
- Updated `DataSeeder.java` to seed default tenants first.
- Added tenant filtering query methods in the respective JpaRepositories.
- Updated `InventoryController.java`, `ProcurementController.java`, `HRController.java`, and `PosController.java` to filter listings and validate modifications by the logged-in user's tenant ID.
- Successfully compiled the project using `mvn clean compile` (`BUILD SUCCESS`).
